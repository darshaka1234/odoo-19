<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="ir_cron_expire_car_reservations" model="ir.cron">
        <field name="name">Car Reservation: Expire Old Reservations</field>
        <field name="model_id" search="[('model', '=', 'car.reservation')]"/>
        <field name="type">ir.actions.server</field>
        <field name="state">code</field>
        <field name="code">model._cron_expire_reservations()</field>
        <field name="interval_number">1</field>
        <field name="interval_type">hours</field>
        <field name="numbercall">-1</field>
        <field name="doall">False</field>
    </record>
</odoo>